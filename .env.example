# Optional. If unset, the server uses the free OpenStreetMap / OSRM
# fallback for all geocoding and routing tools. Set this JWT (signed
# with your MapKit private key) to use the Apple Maps Server API instead.
# See README.md → "Generating the JWT" for instructions.
APPLE_MAPS_AUTH_TOKEN=
